specVersion: "v1beta"
serviceId: "waterly"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "waterly-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
